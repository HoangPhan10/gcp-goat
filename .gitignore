# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.backup

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data,
# except for .tfvars.example files, which are typically used to illustrate
# how to set up variables.
# *.tfvars
# !*.tfvars.example

# Ignore override files as they are usually used for local testing or customization.
*.override.tf
*.override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*
*.tfplan

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Terraform lock file
.terraform.lock.hcl